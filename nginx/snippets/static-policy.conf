# Static assets: aggressive caching, safe for content-hashed deployments.
# In this kata, assets are static files, so caching is safe and improves performance.

expires 1y;
add_header Cache-Control "public, max-age=31536000, immutable" always;

# Improve cache freshness correctness.
add_header Pragma "";
add_header ETag "";
